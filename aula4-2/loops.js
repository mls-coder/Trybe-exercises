//tabuada

let num = 1;

for (let index = 1; index <= 10; index += 1) {
    for(let indexInterno = 1; indexInterno <= 10; indexInterno += 1) {
        console.log(index + ' * ' + indexInterno + ' = ' + index * indexInterno);
    }
}